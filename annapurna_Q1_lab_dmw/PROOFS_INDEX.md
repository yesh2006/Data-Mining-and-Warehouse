# Proof index

| Task | Main proof files |
|---|---|
| A | `proofs/A/` — containers, PostgreSQL master counts, MinIO bucket, DuckDB, successful ETL, partition pruning |
| B | `proofs/B/B1_idempotency_three_runs.png` |
| C | `proofs/C/` — dimensions, date dimension, dashboard query, October revenue |
| D | `proofs/D/` — March/October temporal price evidence |
| E | `proofs/E/` — federated execution plan and scan/join evidence |
| F | `proofs/F/` — full reconciliation, differing months, October check, finance CSV schema |

All screenshots are from the completed local run. Raw input data and the `truth.json` ground-truth file are not included in this GitHub pack.
