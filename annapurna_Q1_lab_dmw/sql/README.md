# SQL files

- `task_a_s01_october.sql` — store/month query and partition-pruning proof
- `task_b_verify.ps1` — three-run idempotency result table
- `task_c_schema.sql` — dashboard dimensions and indexes
- `task_c_dashboard.sql` — dashboard slice and October check
- `task_d_historical_price.sql` — temporal price query
- `task_e_cross_system.sql` — federated MinIO + PostgreSQL query
- `task_f_reconcile.sql` — monthly finance reconciliation

Replace the `<...>` placeholders with local environment values; do not commit real credentials.
